'''
    function select_artist():
        - show all the artists (using the function artists from the db module)
        - ask to input an artist id
        - check if the selected id is in the list
        - if not, return -1
        - if so, return the id of that artist
'''
def select_artist(db):

    list_artists = db.artists()

    # add your code here


    return selected_id
