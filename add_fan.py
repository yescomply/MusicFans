'''
    function add_fan():
        - ask to select an artist (using the function select_artist from the select_artist module)
        - ask to select a fan (using the function select_fan from the select_fan module)
        - check if the selected fan is not already a fan of the selected artist (using the function get_fans from the get_fans module)
        - calls the function add_fan if the fan should be added (using the function add_fan from the db module)
'''
def add_fan(db):

    # add your code here

    db.add_fan(self, artist_id, fan_id)
    return 0
