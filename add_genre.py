'''
    function add_genre():
        - ask to input the label of the genre
        - check if the genre already exists (using the function genres in the db module)
        - calls the function add_genre
'''
def add_genre(db):

    # add your code here

    db.add_genre(label)
    return 0
